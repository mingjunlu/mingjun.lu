---
import { siteVersion, siteUrl } from 'src/constants/site';
---

<script
  is:inline
  async
  src={`/umm.${siteVersion}.min.js`}
  data-website-id={import.meta.env.PUBLIC_ANALYTICS_ID}
  data-domains={new URL(siteUrl).hostname}></script>
